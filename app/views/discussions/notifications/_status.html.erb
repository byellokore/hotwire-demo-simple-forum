<h4>Notifications</h4>
<p><%= discussion.subscribed_reason(user) %></p>
<% if discussion.subscribed?(user) %>
  <%= button_to "Unsubscribe", discussion_notifications_path(discussion), method: :post, class: "btn btn-primary btm-sm mb-2" %>
<% else %>
  <%= button_to "Subscribe", discussion_notifications_path(discussion), method: :post, class: "btn btn-success btm-sm mb-2" %>
<% end %>
